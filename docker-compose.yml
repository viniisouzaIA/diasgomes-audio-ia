services:
  app:
    build: .
    container_name: diasgomes-audio-ia
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
